import Team from './basic';

Team();
/* const roundPlayers = new Team();
roundPlayers.toArray();
const player = {
    name: '1',
    type: 'Daemon',
    health: 10,
    level: 1,
    attack: 10,
    defence: 40,
  };

roundPlayers.add({
    name: '2',
    type: 'Daemon',
    health: 100,
    level: 1,
    attack: 10,
    defence: 40,
  });
  roundPlayers.add(player);

  roundPlayers.addAll({
    name: '3',
    type: 'Daemon',
    health: 10,
    level: 1,
    attack: 10,
    defence: 40,
  },
  {
    name: '4',
    type: 'Daemon',
    health: 10,
    level: 1,
    attack: 10,
    defence: 40,
  },
  player,
  );

  roundPlayers.addAll({
    name: '5',
    type: 'Daemon',
    health: 10,
    level: 1,
    attack: 10,
    defence: 40,
  },
  player,
  );

console.log(roundPlayers.members);
console.log(roundPlayers.toArray());

*/
